This is a [Next.js](https://nextjs.org) project bootstrapped with [`create-next-app`](https://nextjs.org/docs/app/api-reference/cli/create-next-app).

## Getting Started

First, run the development server:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
# or
bun dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

# code structure

app folder is meant to only hold the routing logic
components/pages should have actual page level code
nav bar is just a component and not part of page right ?

# fonts

we will use 2 fonts here

1. tai heritage for logo nd stuff -> serif font looks elegant
2. aleo for the site -> sans serif font make the site look moden
