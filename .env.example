# Copy to .env and fill in for local `vercel dev`. Never commit the real .env file.

# Incoming webhook URL (e.g. a Slack incoming webhook) that receives new
# "Book an appointment" form submissions from api/book-appointment.js.
APPOINTMENTS_WEBHOOK_URL=
